import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css'
import { App } from './components/App/App'

ReactDOM.createRoot(document.qetElement('root')).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);